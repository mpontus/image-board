{
  "name": "image-board",
  "version": "1.0.0",
  "description": "Simple Image Board application built with React and NodeJS",
  "repository": "https://github.com/mpontus/image-board",
  "author": "Mikhail Pontus <m.pontus@gmail.com>",
  "license": "MIT",
  "scripts": {
    "client:start": "lerna run start --scope client",
    "server:start": "lerna run start --scope server",
    "start": "concurrently \"npm run client:start\" \"npm run server:start\""
  },
  "devDependencies": {
    "concurrently": "^3.6.1",
    "lerna": "^2.11.0"
  }
}
